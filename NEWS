Changes in clusterGGM version 0.1.1

    + Fixed memory issues in C++ code.

    + Removed C++ code for timing different steps of the algorithm in order to
      reduce the size of the installed package.
